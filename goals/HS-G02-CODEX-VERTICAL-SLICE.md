# HS-G02 — Codex Vertical Slice

Implement the smallest admitted Codex evidence → canonical HookInvocation → deterministic machine-readable report path.

Required: stable handler identity, runtime-native terminal normalization, explicit coverage, malformed-record isolation, sanitized fixtures, and a CLI/JSON output sufficient to verify counts. No TUI dependency is required yet. Do not ingest or store raw prompts/tool payloads.
